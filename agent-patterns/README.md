# agent-patterns — reusable Effect/Schema idioms

When writing Effect code, inspect `repos/effect/` + `repos/effect/LLMS.md`
and capture reusable idioms here (e.g. `effect-schema.md`).
Do NOT import from `repos/**` in app code.
