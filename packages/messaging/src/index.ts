/**
 * `@rawr/messaging` — `CoinUpdated` PubSub and RabbitMQ adapter.
 *
 * Skeleton placeholder (Phase 0). Phase 1 adds the `CoinUpdated` event
 * schema, an in-process `PubSub` layer, and the RabbitMQ publisher /
 * subscriber with `Schedule` reconnect. All payloads decode via `Schema`
 * at the AMQP edge.
 *
 * @module
 */

/** Package identifier for the messaging skeleton. */
export const packageName = "@rawr/messaging" as const;
