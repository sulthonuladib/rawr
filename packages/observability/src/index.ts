/**
 * `@rawr/observability` — logging, tracing and `LogId`.
 *
 * Skeleton placeholder (Phase 0). Replaces the legacy `crawler-logs` AMQP
 * queue with a structured `LogId` carried through `Effect` annotations.
 * No secrets in logs.
 *
 * @module
 */

/** Package identifier for the observability skeleton. */
export const packageName = "@rawr/observability" as const;
