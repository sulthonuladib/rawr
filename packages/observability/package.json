{
  "name": "@rawr/observability",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Logging, tracing and LogId (replaces legacy crawler-logs queue)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "dev": "tsc --watch -p tsconfig.json",
    "test": "echo \"ok: no tests yet\""
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
