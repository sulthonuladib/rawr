/**
 * `@rawr/config` — typed environment and service configuration.
 *
 * Skeleton placeholder (Phase 0). Real implementation provides
 * `Schema`-validated `Config` with `Redacted` secrets; app code must
 * read configuration here instead of `process.env` sprawl.
 *
 * @module
 */

/** Package identifier for the config skeleton. */
export const packageName = "@rawr/config" as const;
