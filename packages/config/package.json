{
  "name": "@rawr/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Typed environment and service configuration (Schema-validated, Redacted secrets)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "dev": "tsc --watch -p tsconfig.json",
    "test": "echo \"ok: no tests yet\""
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
