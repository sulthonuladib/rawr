{
  "name": "@rawr/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared domain Schemas and errors (CmcId, ActiveCoin, CoinUpdated, ...)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "dev": "tsc --watch -p tsconfig.json",
    "test": "echo \"ok: no tests yet\""
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
