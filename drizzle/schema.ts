/**
 * Drizzle schema entrypoint.
 *
 * Skeleton placeholder (Phase 0). Phase 1 adds `pgTable` definitions for
 * `active_coins`, `exchange_symbols`, `orderbook_snapshots`, and
 * `opportunities` (snake_case). Generate migrations with
 * `pnpm db:generate`, apply with `pnpm db:migrate`.
 *
 * @module
 */

/** Schema version for the drizzle placeholder (replaced by real tables in Phase 1). */
export const schemaVersion = 0 as const;
