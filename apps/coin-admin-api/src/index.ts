/**
 * `@rawr/coin-admin-api` — coin admin HTTP API.
 *
 * Skeleton placeholder (Phase 0). Phase 2 adds an `HttpApi` with
 * `PUT` / `PATCH /coins/:cmcId`: transactional update followed by a
 * `CoinUpdated` publish. Replaces the legacy 30+ key `PUT` and the
 * `"0.1"` string bug with `Schema` decoding at the edge.
 *
 * @module
 */

/** Package identifier for the coin-admin-api skeleton. */
export const packageName = "@rawr/coin-admin-api" as const;
