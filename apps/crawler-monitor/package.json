{
  "name": "@rawr/crawler-monitor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Crawler monitor — log fan-out with LogId",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "dev": "tsc --watch -p tsconfig.json",
    "test": "echo \"ok: no tests yet\""
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
