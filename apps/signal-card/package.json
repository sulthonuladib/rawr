{
  "name": "@rawr/signal-card",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Signal card — Effect HttpServer with real HTML templates (not nginx static)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "dev": "tsc --watch -p tsconfig.json",
    "test": "echo \"ok: no tests yet\""
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
