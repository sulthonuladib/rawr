/**
 * `@rawr/signal-card` — signal card web server.
 *
 * `Effect` `HttpServer` rendering real HTML templates for the table /
 * makerman / management / services / monitor views.
 *
 * @module
 */

/** Package identifier for the signal-card skeleton. */
export const packageName = "@rawr/signal-card" as const
