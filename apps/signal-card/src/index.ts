/**
 * `@rawr/signal-card` — signal card web server.
 *
 * Skeleton placeholder (Phase 0). Phase 3 rewrites this as an `Effect`
 * `HttpServer` rendering real HTML templates for the table / makerman /
 * management / services / monitor views (not nginx static).
 *
 * @module
 */

/** Package identifier for the signal-card skeleton. */
export const packageName = "@rawr/signal-card" as const;
