/**
 * `@rawr/gate-proxy` — thin authenticated proxy.
 *
 * Skeleton placeholder (Phase 0). Phase 2 forwards allow-listed routes
 * with `Schema` header / body decoding and `Redacted` credentials
 * (replaces legacy `10001:42069` proxy).
 *
 * @module
 */

/** Package identifier for the gate-proxy skeleton. */
export const packageName = "@rawr/gate-proxy" as const;
