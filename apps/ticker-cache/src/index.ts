/**
 * `@rawr/ticker-cache` — scheduled ticker snapshot cache.
 *
 * Skeleton placeholder (Phase 0). Phase 2 refreshes cached tickers on a
 * `Schedule` (replaces legacy `:4001` service) so hot reads never hit
 * Postgres directly.
 *
 * @module
 */

/** Package identifier for the ticker-cache skeleton. */
export const packageName = "@rawr/ticker-cache" as const;
