{
  "name": "rawr",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Crypto arbitrage signals across 15 exchanges — Effect monorepo",
  "engines": {
    "node": "^24"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.22.0",
      "onFail": "download"
    }
  },
  "scripts": {
    "dev": "pnpm --recursive --parallel run dev",
    "check": "pnpm --recursive run check",
    "lint": "oxlint",
    "test": "pnpm --recursive run test",
    "build": "pnpm --recursive run build",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "devDependencies": {
    "@oxlint/plugins": "catalog:",
    "@types/node": "catalog:",
    "drizzle-kit": "catalog:",
    "oxlint": "catalog:",
    "pg": "catalog:",
    "typescript": "catalog:"
  }
}
